export const CIRCLE_SIZE = 48;
export const HALF_CIRCLE_SIZE = CIRCLE_SIZE / 2;

export const SNAP_DELTA = 8;

export enum SelectorMoveType {
  NONE = 0,
  BOUNDING_BOX = 1,
  TOP_LEFT = 2,
  BOTTOM_RIGHT = 3,
}
